# 基于大模型的新闻内容采编系统项目总文档

## 一、项目基本信息

项目名称：NewsFinshied，基于大模型的新闻内容采编系统。

项目类型：毕业设计项目、前后端分离 Web 应用、新闻采编业务系统。

技术栈：

| 层级 | 技术 |
|------|------|
| 前端 | React 18、TypeScript、Vite、Ant Design 5 |
| 后端 | FastAPI、SQLAlchemy 2.0、Pydantic v2 |
| 数据库 | MySQL 8.0 |
| AI 服务 | DeepSeek、SiliconFlow、OpenAI 兼容协议 |
| 认证 | JWT Token |
| 采集与解析 | curl_cffi、BeautifulSoup、RSS/XML/JSON 解析 |
| 本地运行 | `./start.sh` 统一启动 |

本项目面向新闻采编场景，围绕“线索采集、选题策划、AI 稿件生成、稿件管理、人工审核、发布展示、读者反馈”构建完整业务流程。系统不是单独的 AI 写作页面，也不是普通 CMS，而是把新闻生产链路中的多个环节串成一个可演示、可扩展、可落库的闭环。

## 二、项目背景与研究意义

传统新闻采编工作中，新闻线索往往分散在多个网站、热榜、RSS、公开接口或人工搜索渠道中。编辑需要不断切换平台，筛选素材、整理信息、撰写初稿和提交审核，整体流程耗时较长。

同时，普通内容管理系统大多只负责文章发布和内容管理，对“线索发现”和“AI 辅助写稿”支持不足；单点 AI 写作工具虽然可以生成文本，但缺少稿件状态管理、审核记录、发布展示和读者反馈能力。

本项目引入大模型能力，将 AI 放入新闻生产流程中，用于线索分析、标题生成、摘要生成、稿件初稿生成、内容润色和审核预审。系统通过前后端分离架构和 MySQL 数据库存储，实现从线索到发布再到反馈的完整业务闭环。

项目意义主要体现在：

- 提高新闻线索发现效率，减少人工搜索和筛选成本。
- 降低稿件初稿、摘要、标题、关键词等重复写作负担。
- 规范稿件状态流转和审核意见记录。
- 保留人工审核机制，使 AI 作为辅助工具而非完全替代人工。
- 为后续内容优化提供浏览、点赞、评论等反馈数据基础。

## 三、系统面向对象

系统主要面向以下用户角色：

| 用户对象 | 主要需求 |
|----------|----------|
| 新闻编辑 / 记者 | 采集线索、整理素材、生成初稿、管理稿件 |
| 审核人员 | 查看待审稿件、参考 AI 预审结果、提交审核意见 |
| 管理员 | 查看用户角色、人员数据和系统业务概况 |
| 普通读者 | 浏览已发布新闻、搜索文章、点赞、评论和反馈 |

从毕业设计角度看，本项目核心服务对象是新闻采编机构、校园媒体团队或轻量内容生产团队，重点解决内容生产效率、流程协同和智能辅助不足的问题。

## 四、主要解决的问题

### 1. 线索发现分散

新闻线索来源复杂，可能来自新闻网站、搜索引擎、RSS、公开接口、视频平台热榜或社交平台。系统通过新闻线索模块将可采集来源集中到同一页面，并将采集结果转换为统一的线索数据。

### 2. 稿件初稿耗时

新闻写作需要整理素材、拟定标题、撰写导语、组织正文和提炼摘要。系统通过 AI 稿件生成模块，根据主题、关键词、参考素材和用户自定义提示词生成稿件初稿，降低起稿成本。

### 3. 审核流程不透明

传统审核可能依赖线下沟通，状态和意见不易追踪。系统通过稿件状态和审核记录表保存审核结果、审核意见和状态变化，使稿件流转更加清晰。

### 4. 生产与反馈脱节

普通 CMS 往往关注发布，较少把读者反馈反向提供给采编工作。系统保留浏览量、点赞量、评论量等反馈数据，为后续内容优化和选题判断提供基础。

## 五、总体架构

系统采用前后端分离架构，前端负责页面展示和交互操作，后端提供 REST API，数据库负责持久化存储，大模型服务负责 AI 生成、润色和预审。

```text
用户浏览器
  |
  |  React + TypeScript + Ant Design
  v
前端应用 frontend
  |
  |  Axios / REST API / JWT Token
  v
FastAPI 后端 backend
  |
  |  SQLAlchemy ORM
  v
MySQL 数据库

FastAPI 后端
  |
  |  OpenAI 兼容协议
  v
DeepSeek / SiliconFlow / OpenAI 等大模型服务
```

架构特点：

- 前后端分离：前端和后端通过 REST API 通信，职责清晰。
- 分角色页面：采编端、审核端、读者端、管理员端分开设计。
- 数据统一入库：用户、线索、选题、稿件、审核、反馈都存入 MySQL。
- AI 能力服务化：后端统一封装 LLM 调用，便于切换不同模型服务。
- 本地启动统一：项目主要通过 `./start.sh` 检查环境、初始化数据库并启动前后端。

## 六、系统目录结构

```text
news_finshied/
├── backend/                  # FastAPI 后端
│   ├── app/
│   │   ├── main.py           # 应用入口和路由注册
│   │   ├── database.py       # SQLAlchemy 数据库连接
│   │   ├── core/             # 配置、认证、安全能力
│   │   ├── models/           # SQLAlchemy 数据模型
│   │   ├── routers/          # API 路由
│   │   ├── schemas/          # Pydantic 请求响应模型
│   │   ├── services/         # AI、业务服务、缓存、限流
│   │   └── migrations/       # 表结构补丁和初始化逻辑
│   ├── init.sql              # MySQL 初始化脚本
│   └── requirements.txt      # Python 依赖
├── frontend/                 # React + TypeScript 前端
│   ├── src/
│   │   ├── pages/            # 页面
│   │   ├── components/       # 公共组件
│   │   ├── services/         # API 请求封装
│   │   ├── hooks/            # 自定义 Hooks
│   │   ├── utils/            # 工具函数
│   │   └── constants/        # 路由和常量
│   └── package.json
├── img/                      # 项目截图
├── uploads/                  # 上传文件目录
├── start.sh                  # 本地启动脚本
├── README.md                 # GitHub 展示说明
├── 架构文档.md               # 架构与答辩说明
├── 对象问题创新点.md         # 对象、问题和创新点
├── 调整清单.md               # 后续调整清单
└── 项目总文档.md             # 汇总后的总文档
```

## 七、后端设计

后端基于 FastAPI，按路由、模型、Schema、服务层进行拆分。`backend/app/main.py` 负责创建应用、注册中间件和挂载路由。

主要 API 模块如下：

| 模块 | 路由前缀 | 说明 |
|------|----------|------|
| 认证 | `/api/auth` | 登录、注册、当前用户信息 |
| 用户 | `/api/users` | 用户资料、密码、头像、管理员用户查看 |
| 线索 | `/api/clues` | 线索列表、采集、搜索资料、AI 分析 |
| 选题 | `/api/topics` | 选题创建、状态管理、稿件关联 |
| 稿件 | `/api/articles` | 稿件增删改查、提交审核、发布、下线 |
| 内容生成 | `/api/content` | AI 写稿、标题生成、摘要、关键词、AI 预审 |
| 审核 | `/api/reviews` | 待审队列、审核记录、审核结果提交 |
| 反馈 | `/api/feedback` | 阅读、点赞、分享、评论和趋势统计 |
| 公开访问 | `/api/public` | 读者端文章列表、文章详情和评论 |
| 消息 | `/api/reader/messages` | 读者消息、未读数、标记已读、回复 |
| 统计 | `/api/stats` | 首页统计和仪表盘数据 |
| 文本处理 | `/api/text` | 翻译、摘要、润色、扩写、提取 |

后端优势：

- FastAPI 提供高性能 API 和自动 Swagger 文档。
- Pydantic 用于请求和响应数据校验。
- SQLAlchemy 统一管理数据库模型和查询。
- JWT 用于登录鉴权。
- AI 调用统一封装在服务层，便于切换模型供应商。

## 八、前端设计

前端基于 React 18、TypeScript、Vite 和 Ant Design 5，属于典型管理型、交互型应用。

前端主要分为四类页面：

| 页面类型 | 说明 |
|----------|------|
| 采编端 | 首页、新闻线索、选题策划、AI 稿件生成、稿件管理、数据统计 |
| 审核端 | 审核首页、待审队列、发布管理、审核员资料 |
| 读者端 | 文章首页、分类、搜索、文章详情、消息中心 |
| 认证页 | 登录、注册 |

选择 React 的原因：

- 组件化能力强，适合拆分线索表格、稿件表单、审核队列等复杂页面。
- 状态驱动 UI，适合稿件状态切换、弹窗、抽屉、分页筛选等交互。
- 配合 Ant Design 可以快速构建统一风格的后台系统。
- TypeScript 能减少前后端字段不一致带来的错误。
- React Router、Axios 等生态成熟，适合前后端分离项目。

答辩时可以这样表述：

> 我选择 React 是因为本系统不是静态展示页面，而是一个多角色、多状态、多表单的采编管理系统。React 的组件化和状态管理方式更适合构建这类复杂交互页面，同时 Ant Design 能快速提供表格、表单、弹窗、抽屉等成熟组件，提高开发效率和界面一致性。

## 九、数据库设计

项目使用 MySQL 作为主要数据库，通过 SQLAlchemy ORM 操作。

核心数据表如下：

| 表名 | 作用 |
|------|------|
| `users` | 用户账号、角色、邮箱、昵称、启用状态 |
| `clues` | 新闻线索、来源、摘要、关键词、评分、创建者 |
| `topics` | 选题策划、负责人、状态、AI 评分 |
| `articles` | 新闻稿件、正文、摘要、分类、状态、作者 |
| `reviews` | 审核记录、审核人、审核结果、审核意见 |
| `feedbacks` | 阅读量、点赞量、评论量、分享量、热度数据 |
| `comments` | 文章评论 |
| `messages` | 系统消息、通知、回复、未读状态 |
| `collections` | 采集任务和采集状态 |

数据库连接配置在 `backend/app/database.py` 中。当 `DATABASE_URL` 使用 MySQL 地址时，系统使用连接池参数，例如：

- `pool_size=10`
- `max_overflow=20`
- `pool_pre_ping=True`
- `pool_recycle=3600`

这样可以减少频繁创建连接的开销，并避免 MySQL 空闲连接断开导致接口失败。

## 十、核心业务流程

### 1. 新闻生产流程

```text
新闻线索采集
  ↓
线索入库并计算基础评分
  ↓
编辑选择线索或手动输入主题
  ↓
AI 生成标题、正文、摘要和写作角度
  ↓
保存为稿件草稿
  ↓
提交审核
  ↓
审核员查看 AI 预审分和稿件详情
  ↓
人工审核通过 / 退回 / 拒绝
  ↓
发布到读者端
  ↓
读者浏览、点赞、评论，形成反馈数据
```

### 2. 稿件状态流转

系统中的稿件状态主要包括：

```text
draft 草稿
  → pending_review 待审核
  → reviewing 审核中
  → approved 已通过
  → published 已发布

异常状态：
pending_review / reviewing
  → rejected 已拒绝
  → draft 退回修改
```

当前实现中，审核接口在审核通过时可以直接将文章状态更新为 `published`，用于演示“审核通过并发布”的闭环；同时系统中保留 `approved` 状态，作为已通过但未发布的中间状态和前端兼容状态。

### 3. 读者反馈流程

```text
读者打开文章
  ↓
系统记录浏览量
  ↓
读者点赞 / 评论 / 分享
  ↓
反馈数据写入 feedbacks 和 comments
  ↓
采编端统计页面聚合展示
  ↓
为后续选题和内容优化提供参考
```

## 十一、AI 能力实现

后端通过 `backend/app/services/llm.py` 统一封装大模型调用。业务代码不直接绑定某一个模型，而是通过统一客户端调用 DeepSeek、SiliconFlow、OpenAI 或其他兼容 OpenAI 协议的模型服务。

常见配置项包括：

- `DEEPSEEK_API_KEY`
- `DEEPSEEK_BASE_URL`
- `SILICONFLOW_API_KEY`
- `SILICONFLOW_BASE_URL`
- `OPENAI_API_KEY`
- `AI_MODEL`
- `AI_TIMEOUT`

项目中真实 API Key 不应提交到仓库，只保留 `.env.example` 示例文件。

### 1. AI 稿件生成

AI 写稿接口：

```text
POST /api/content/generate
```

前端页面：

```text
frontend/src/pages/editor/AIArticle.tsx
```

发送给后端的数据包括：

- `topic`：主题或标题。
- `keywords`：关键词。
- `style`：文章风格。
- `length`：文章长度。
- `title_count`：备选标题数量。
- `tone`：语调。
- `audience`：目标受众。
- `reference_material`：参考素材。
- `custom_prompt`：用户额外提示词。

后端将这些字段拼接成 prompt，再调用大模型。模型需要返回 JSON：

```json
{
  "suggested_titles": ["标题1", "标题2", "标题3"],
  "draft_content": "完整稿件正文",
  "summary": "摘要",
  "recommended_angles": ["角度1", "角度2"],
  "structure_suggestion": "结构建议"
}
```

如果大模型没有返回合法 JSON，后端会把原始文本作为稿件正文返回，保证页面仍能显示结果。

### 2. 自定义提示词

AI 稿件生成页面支持额外提示词。用户可以在“AI 提示词（可选）”中输入自己的写作要求，例如：

- 希望文章更正式。
- 希望突出某个角度。
- 希望控制段落结构。
- 希望避免夸张标题。
- 希望更适合校园媒体或政务新闻表达。

这些内容会作为 `custom_prompt` 字段传给后端，后端追加到最终 prompt 中，与主题、关键词和参考素材一起发送给大模型。

答辩时可以这样说：

> 系统不是固定模板式生成，而是允许用户在生成前输入额外提示词。这个提示词会和线索内容、关键词、风格、长度等参数一起发送给大模型，因此用户可以控制写作方向、语气和注意事项。

### 3. AI 审核预审

AI 预审接口：

```text
POST /api/content/ai-review
```

后端会根据 `article_id` 查询稿件标题、摘要和正文，然后构造审核提示词，让大模型从以下维度进行判断：

- 事实准确性：是否有明显事实错误。
- 语言规范：语法、用词是否规范。
- 结构逻辑：文章结构是否清晰合理。
- 新闻价值：选题是否有新闻价值。

AI 需要返回 JSON：

```json
{
  "result": "pass/reject/revision",
  "score": 80,
  "comment": "审核意见",
  "issues": ["问题1"],
  "suggestions": ["建议1"]
}
```

需要说明的是，AI 审核不是最终自动发布机制。系统将 AI 审核定位为辅助预审，用来给审核员提供评分和建议，最终审核结果仍由人工审核员确认。为了保证系统可演示和可用，项目也保留了接口兜底结果和前端临时评分展示。

答辩时推荐说法：

> 项目实现了 AI 预审接口，会调用大模型进行内容审核；同时为了保证系统演示稳定，保留了兜底评分和默认结果。最终发布仍由人工审核员确认，AI 的定位是辅助审核，不是完全替代人工。

## 十二、新闻线索采集实现

线索采集主要在：

```text
backend/app/routers/clues.py
```

采集接口：

```text
GET /api/clues/collect/multichannel
POST /api/clues/collect/multichannel
```

当前保留的稳定信源包括：

- 腾讯新闻。
- 新浪新闻。
- 网易新闻入口。
- 知乎日报。
- B 站热门。
- Bing 搜索。
- Bing 社交定向搜索。
- Bing 政府网站定向搜索。

采集方式包括：

- RSS / XML 解析。
- JSON API 解析。
- HTML 搜索结果解析。
- Bing RSS 兜底。

系统会将外部来源统一转换成线索数据模型：

- `title`：线索标题。
- `content`：线索摘要或标题。
- `source`：来源名称。
- `source_url`：来源链接。
- `keywords`：关键词。

AI 写稿时，系统不会直接把网页、视频或杂乱 HTML 原文发送给 AI，而是先把采集结果清洗为结构化文字字段，再作为 `reference_material` 发送给 AI。

如果线索来自视频平台，例如 B 站热门，当前系统主要采集视频标题、链接、播放量、弹幕量等基础信息，不下载视频，也不做语音转文字。如果后续扩展，可以加入字幕提取或 ASR 语音识别，把视频内容转成文本后再喂给 AI。

答辩时推荐说法：

> 当前版本主要采集线索级信息，包括标题、摘要、来源链接和热度数据。网页正文抽取、视频字幕识别属于后续可扩展方向。

## 十三、主要功能模块

### 1. 采编端

采编端主要服务新闻编辑和记者，包括：

- 新闻线索：采集线索、搜索资料、查看详情、AI 分析。
- 选题策划：创建选题、分配负责人、关联稿件。
- AI 稿件生成：根据主题、关键词、素材和提示词生成稿件。
- 稿件管理：创建、编辑、提交审核、查看状态、发布管理。
- 数据统计：展示稿件、线索、待审、发布等核心指标。

### 2. 审核端

审核端主要服务审核人员，包括：

- 待审队列：集中展示待审核或审核中的稿件。
- AI 预审：展示 AI 评分和审核建议。
- 人工审核：支持通过、拒绝、退回修改。
- 发布管理：查看已发布稿件，必要时下线并填写原因。

### 3. 读者端

读者端主要服务普通用户，包括：

- 文章浏览：展示已发布新闻。
- 分类与搜索：支持按分类、标题和内容检索。
- 文章详情：阅读正文，查看发布时间和分类。
- 互动反馈：支持浏览统计、点赞、评论、分享等能力。
- 消息中心：展示系统通知和回复消息。

### 4. 管理员端

管理员端主要用于查看系统人员数据，包括：

- 全部用户。
- 管理员。
- 审核员。
- 记者。
- 编辑。
- 投稿用户。

管理员可以查看用户姓名、昵称、账号、角色、邮箱、启用状态、加入时间、稿件数、发布数、待审核数、草稿数、线索数和审核数。

## 十四、项目创新点

### 1. 大模型深度融入采编流程

系统不是简单调用 AI 生成一段文字，而是在多个环节使用大模型能力：

- 线索阶段：辅助判断新闻价值和传播潜力。
- 写作阶段：辅助生成标题、摘要、关键词和正文。
- 修改阶段：辅助续写、改写、润色。
- 审核阶段：辅助给出内容质量和风险判断。

### 2. 全流程闭环设计

系统覆盖新闻生产的完整链路：

```text
线索发现 → 选题策划 → AI 写稿 → 稿件管理 → 审核发布 → 读者反馈
```

相比单独的 AI 写作工具或普通文章管理后台，本项目更贴近真实采编流程。

### 3. 三端分离

系统区分采编端、审核端和读者端：

- 采编端负责内容生产。
- 审核端负责质量控制。
- 读者端负责内容展示和反馈。

三类角色职责清晰，方便答辩时展示权限分离和业务流程设计。

### 4. 线索采集与 AI 写稿结合

系统将外部新闻线索采集结果统一转换为结构化文字数据，再作为 AI 写稿的参考素材，实现从素材发现到稿件初稿生成的衔接。

### 5. 轻量化部署与演示

项目使用 FastAPI、React、MySQL 和 Docker Compose，既能体现完整工程架构，也能在普通开发环境中通过脚本快速启动，适合毕业设计演示和后续扩展。

## 十五、系统安全与配置

项目使用 JWT Token 进行登录鉴权。前端登录后保存 Token，后续请求通过 Axios 携带 Token 调用后端接口。

项目中不能提交真实密钥。GitHub 展示和答辩时应只提交：

- `.env.example`
- `backend/.env.example`
- `frontend/.env.example`

真实配置应保存在本地 `.env` 文件中，不上传仓库。

重要配置包括：

- `DATABASE_URL`
- `SECRET_KEY`
- `DEEPSEEK_API_KEY`
- `SILICONFLOW_API_KEY`
- `OPENAI_API_KEY`
- `AI_MODEL`
- `CORS_ORIGINS`

答辩或展示时需要注意：

- 不展示真实 API Key。
- 不展示本地数据库密码。
- 不提交上传文件、构建产物、依赖目录和缓存文件。
- 说明 `.gitignore` 已排除 `.env`、`node_modules`、`dist`、缓存目录等。

## 十六、本地运行方式

本地运行主要使用：

```bash
./start.sh
```

脚本负责：

- 检查 Python 环境。
- 检查 Node 环境。
- 检查 MySQL 状态。
- 初始化或迁移数据库表。
- 启动 FastAPI 后端。
- 启动 Vite 前端。

默认访问地址：

```text
前端：http://localhost:3000
后端：http://localhost:8000
Swagger：http://localhost:8000/docs
```

如果手动启动，也可以分别进入后端和前端目录执行：

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

```bash
cd frontend
npm install
npm run dev
```

## 十七、答辩演示流程

建议答辩时优先展示最完整、最能体现项目主题的主流程：

1. 登录采编端。
2. 进入新闻线索页面，采集或查看新闻线索。
3. 选择线索，进入 AI 稿件生成页面。
4. 输入主题、关键词、参考素材和自定义提示词。
5. 调用 AI 生成标题、正文、摘要和写作角度。
6. 将生成内容保存为稿件草稿。
7. 在稿件管理中提交审核。
8. 登录审核端，查看待审稿件。
9. 查看 AI 预审分和建议，人工审核通过。
10. 在读者端查看已发布文章。
11. 进行点赞、评论或浏览反馈展示。

这个流程最能体现系统主题：

```text
大模型支持的新闻内容采编系统
```

## 十八、答辩常见问题回答

### 1. 为什么选择 React？

本系统是一个多角色、多表格、多状态、多表单的管理型应用，不是静态展示页面。React 的组件化和状态驱动 UI 更适合构建复杂交互页面，同时 Ant Design 能提供成熟的表格、表单、弹窗和抽屉组件，提高开发效率和界面一致性。

### 2. 为什么选择 FastAPI？

FastAPI 性能较好，接口开发效率高，支持 Pydantic 自动校验和 Swagger 文档，非常适合前后端分离、接口较多的业务系统。

### 3. 为什么使用 MySQL？

新闻采编系统包含用户、线索、选题、稿件、审核、反馈等多类结构化数据，并且需要状态流转和关联查询，所以使用 MySQL 做持久化存储，比只用本地文件更可靠。

### 4. AI 审核是不是 mock？

系统实现了 AI 预审接口，会调用大模型对稿件进行评分和建议；同时为了保证演示稳定，保留了兜底评分和默认结果。最终审核结果仍由人工审核员确认，AI 是辅助审核，不是完全自动审核。

### 5. 视频线索如何生成稿件？

当前版本对视频类线索主要采集标题、链接、播放量、弹幕量等线索级信息，不下载视频，也不做语音转文字。生成稿件时，AI 使用这些文字摘要和用户补充提示词作为参考。后续可以扩展字幕提取或 ASR 语音识别。

### 6. 网页线索如何喂给 AI？

系统不会直接把杂乱网页或 HTML 发给 AI，而是先将外部采集结果转换为统一的线索字段，包括标题、摘要、来源、链接和关键词。AI 写稿时使用这些清洗后的文字字段作为参考素材。

### 7. 项目相比普通 CMS 有什么不同？

普通 CMS 主要负责内容发布和管理，本项目强调从线索采集到 AI 写稿、审核发布、读者反馈的完整闭环，并在多个环节引入大模型辅助能力。

### 8. 项目相比单点 AI 写作工具有什么不同？

单点 AI 写作工具通常只负责生成文本，而本项目包含线索采集、稿件管理、审核记录、发布展示和反馈统计，更贴近真实新闻采编流程。

## 十九、当前实现边界

为了答辩表述准确，需要说明当前系统边界：

- 当前不是完整网页正文抽取系统，主要采集标题、摘要、链接等线索级文字。
- 当前不是视频解析系统，暂未做视频下载、字幕提取或语音转文字。
- AI 审核是辅助预审，不是完全自动审核或自动事实核查。
- 读者反馈和统计能力已具备基础实现，后续仍可继续增强趋势分析和可视化。
- 图片上传、消息中心、统计图表等模块可作为后续优化方向继续完善。

## 二十、后续优化方向

后续可以从以下方向继续扩展：

- 网页正文深度抽取，提高线索素材完整度。
- 视频字幕识别或语音转文字，增强视频类新闻线索处理能力。
- AI 审核维度细化，例如敏感词、事实核查、版权风险和标题党判断。
- 多级审核流程，例如初审、复审、终审。
- AI 生成结果溯源，把参考素材和生成段落建立对应关系。
- 更细的数据看板，例如不同记者产出、稿件通过率、线索转化率。
- 完善图片上传能力，包括封面图、头像和稿件插图。
- 优化消息中心，实现更完整的通知、回复和已读状态。

## 二十一、项目总结

本项目面向新闻采编工作中线索分散、写稿耗时、审核流程不透明和生产反馈脱节等问题，设计并实现了一个基于大模型的新闻内容采编系统。

系统采用 React + TypeScript + Ant Design 构建前端，使用 FastAPI + SQLAlchemy + MySQL 构建后端和数据层，通过 OpenAI 兼容协议接入 DeepSeek、SiliconFlow 等大模型服务。系统覆盖新闻线索采集、选题策划、AI 稿件生成、稿件管理、审核发布和读者反馈等环节。

相比普通 CMS，本项目更强调新闻生产流程闭环；相比单点 AI 写作工具，本项目更强调稿件管理、审核追踪和读者反馈。AI 在系统中承担辅助创作、辅助分析和辅助审核的角色，最终内容把关仍由人工完成，符合新闻采编场景中“AI 辅助、人负责”的设计原则。

