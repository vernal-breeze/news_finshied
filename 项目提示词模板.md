# 新闻内容采编系统 - AI 提示词模板

> 基于大模型的新闻内容采编系统 | FastAPI + React 18 + TypeScript + SQLite

---

## 一、项目概述

这是一个面向新闻机构的**全流程智能采编平台**，包含三大端：
- **采编端** (`/editor`): 记者写稿、AI辅助写作、提交审核
- **审核端** (`/review`): 三级审核流程（初审→复审→终审）
- **读者端** (`/reader`): 公开阅读、评论互动

---

## 二、快速提问模板

### 2.1 通用提问框架

```
【角色定位】
你是新闻采编系统的 [后端开发/前端开发/全栈开发/架构师]

【问题背景】
- 当前操作: [描述你正在做什么]
- 预期结果: [描述你希望发生什么]
- 实际结果: [描述实际发生了什么/报错信息]

【环境信息】
- 后端端口: 8000
- 前端端口: 3000
- 相关文件: [列出涉及的文件路径]

【具体请求】
[清晰描述你需要AI做什么]
```

### 2.2 后端开发提问模板

#### API 接口问题
```
【后端问题 - API接口】

端点: [如 POST /api/articles]
问题类型: [404/500/422/数据错误/逻辑错误]

错误信息:
```
[粘贴完整错误日志]
```

相关代码位置:
- Router: backend/app/routers/[文件名].py
- Model: backend/app/models/[文件名].py
- Schema: backend/app/schemas/[文件名].py

请求参数:
```json
[粘贴请求体]
```

请帮我: [定位问题/修复bug/优化代码/添加功能]
```

#### 数据库问题
```
【后端问题 - 数据库】

操作: [查询/插入/更新/删除/迁移]
表名: [如 articles, users, reviews]

SQLAlchemy 报错:
```
[粘贴错误信息]
```

模型定义位置: backend/app/models/[文件名].py

请帮我: [修复模型/优化查询/添加索引/数据迁移]
```

#### AI 服务问题
```
【后端问题 - AI服务】

功能: [标题生成/摘要提取/内容续写/早报生成]
API: SiliconFlow / DeepSeek-V3

报错信息:
```
[粘贴错误]
```

相关文件:
- backend/app/ai_service.py
- backend/app/clue_collector.py

请帮我: [修复调用/优化prompt/添加重试机制]
```

### 2.3 前端开发提问模板

#### React 组件问题
```
【前端问题 - React组件】

页面: [如 /editor/articles, /review/dashboard]
组件路径: frontend/src/pages/[路径].tsx

问题现象: [如白屏/数据不显示/交互无响应/样式错乱]

浏览器控制台报错:
```
[粘贴错误信息]
```

当前代码片段:
```tsx
[粘贴相关代码]
```

请帮我: [修复渲染/修复状态管理/优化性能/添加功能]
```

#### API 调用问题
```
【前端问题 - API调用】

接口: [如 GET /api/articles]
问题: [404/500/数据格式不符/CORS错误]

前端代码位置: frontend/src/[路径].tsx

请求代码:
```typescript
[粘贴 axios/fetch 代码]
```

后端返回:
```json
[粘贴响应数据]
```

请帮我: [修复调用/处理错误/适配数据格式]
```

#### 构建问题
```
【前端问题 - 构建】

命令: npm run [dev/build]
错误信息:
```
[粘贴完整报错]
```

相关文件:
- frontend/vite.config.ts
- frontend/tsconfig.json
- frontend/src/[具体文件]

请帮我: [修复构建错误/优化配置]
```

### 2.4 全栈联调提问模板

```
【全栈联调问题】

功能模块: [如 稿件提交审核 / 消息通知 / 图片上传]

前端表现:
- 页面: [路径]
- 现象: [描述]

后端表现:
- 接口: [端点]
- 状态码: [200/404/500等]
- 响应: [粘贴响应]

数据库:
- 相关表: [表名]
- 当前数据状态: [描述]

请帮我: [定位问题根源/修复端到端流程]
```

---

## 三、按功能模块提问指南

### 3.1 采编端功能

| 功能 | 前端页面 | 后端接口 | 数据库表 |
|------|----------|----------|----------|
| 线索管理 | `/clues` | `/api/clues` | `news_clues` |
| 稿件管理 | `/articles` | `/api/articles` | `articles` |
| AI写作 | `/ai-article` | `/api/content/generate` | - |
| 选题策划 | `/topics` | `/api/topics` | `topics` |
| 提交审核 | 稿件详情页 | `/api/articles/{id}/submit-review` | `articles` |

**提问示例:**
```
【采编端 - 稿件提交审核失败】

我在采编端点击"提交审核"按钮后：
1. 前端显示"提交失败"
2. 后端返回 500 错误

后端日志:
```
[粘贴日志]
```

相关文件:
- 前端: frontend/src/pages/Articles.tsx
- 后端: backend/app/routers/articles.py

请帮我定位问题。
```

### 3.2 审核端功能

| 功能 | 前端页面 | 后端接口 | 说明 |
|------|----------|----------|------|
| 待审队列 | `/review/queue` | `/api/reviews/pending` | 三级审核 |
| 审核操作 | 审核详情页 | `/api/reviews` | 通过/拒绝/退修 |
| 发布管理 | `/review/published` | `/api/articles/{id}/publish` | 终审后发布 |

**审核流程状态机:**
```
draft → pending_review → reviewing → approved → published
         ↓                    ↓           ↓
      need_revision      rejected    archived
```

**提问示例:**
```
【审核端 - 三级审核流程问题】

当前问题: 初审通过后，稿件没有进入复审队列

预期流程: first → second → third → approved
实际流程: first通过后状态卡在reviewing

相关接口:
- GET /api/reviews/pending
- POST /api/reviews

请帮我检查审核层级流转逻辑。
```

### 3.3 读者端功能

| 功能 | 前端页面 | 后端接口 | 问题类型 |
|------|----------|----------|----------|
| 文章列表 | `/reader` | `/api/public/articles` | 数据过滤 |
| 文章详情 | `/reader/article/:id` | `/api/public/articles/:id` | 内容渲染 |
| 评论 | 文章详情页 | `/api/public/comments` | 数据提交/显示 |
| 消息中心 | `/reader/messages` | `/api/reader/messages` | ⚠️ 已知问题 |

**⚠️ 已知问题提醒:**
读者消息中心存在前后端数据结构不一致问题，后端模型缺少 `type`、`is_read` 等字段。

### 3.4 早报生成功能

```python
# 5种早报模板
from app.clue_collector import ClueCollector
c = ClueCollector()

c.collect_daily_briefing(profile='general')   # 综合早报
c.collect_daily_briefing(profile='finance')   # 财经早报
c.collect_daily_briefing(profile='tech')      # 科技早报
c.collect_daily_briefing(profile='ai_daily')  # AI日报
c.collect_daily_briefing(profile='social')    # 社交早报
```

**提问示例:**
```
【早报生成 - 信源采集失败】

运行 collect_daily_briefing(profile='tech') 时：
- HackerNews 信源返回空数据
- GitHub Trending 正常

错误日志:
```
[粘贴日志]
```

相关文件: backend/app/clue_collector.py

请帮我检查 HackerNews API 调用逻辑。
```

---

## 四、常见问题快速定位

### 4.1 启动问题

| 问题 | 检查点 | 解决方向 |
|------|--------|----------|
| 后端启动失败 | `.env` 文件 | 检查 `SECRET_KEY` 和 `SILICONFLOW_API_KEY` |
| 前端构建失败 | `frontend/src/utils/tags.ts` | 该文件包含JSX语法错误 |
| 数据库连接失败 | `DATABASE_URL` | 检查路径和权限 |
| CORS错误 | 后端CORS配置 | 检查 `CORS_ORIGINS` |

### 4.2 功能问题

| 问题 | 定位文件 | 备注 |
|------|----------|------|
| 图片上传无效 | `backend/app/routers/public.py:102` | ⚠️ 占位实现，未完整开发 |
| 消息中心异常 | `backend/app/models/message.py` | ⚠️ 字段缺失 |
| 统计接口无数据 | `backend/app/routers/stats.py` | 部分假数据 |
| 审核流程异常 | `backend/app/routers/reviews.py` | 检查三级审核逻辑 |

---

## 五、技术栈速查

### 5.1 后端技术栈
- **框架**: FastAPI
- **ORM**: SQLAlchemy
- **数据库**: SQLite (开发) / PostgreSQL (生产)
- **认证**: JWT (python-jose + bcrypt)
- **AI API**: SiliconFlow (DeepSeek-V3)
- **Python**: 3.9+

### 5.2 前端技术栈
- **框架**: React 18 + TypeScript
- **构建**: Vite
- **UI库**: Ant Design
- **HTTP**: Axios
- **路由**: React Router

### 5.3 项目结构速查
```
news_editor/
├── backend/
│   ├── app/
│   │   ├── main.py           # 应用入口
│   │   ├── models/           # 数据库模型
│   │   ├── routers/          # API路由
│   │   ├── schemas/          # Pydantic模型
│   │   ├── ai_service.py     # AI服务
│   │   └── clue_collector.py # 线索采集
│   └── data/
│       └── news_editor.db    # SQLite数据库
├── frontend/
│   └── src/
│       └── pages/
│           ├── reader/       # 读者端
│           ├── review/       # 审核端
│           └── *.tsx         # 采编端
└── docs/                     # 文档
```

---

## 六、高效提问技巧

### ✅ 好的提问
```
【后端 - 稿件创建 422 错误】

调用 POST /api/articles 返回 422 Validation Error。

请求体:
```json
{
  "title": "测试标题",
  "content": "测试内容",
  "tags": "科技,AI"  // 这里可能是问题
}
```

后端 Schema (backend/app/schemas/article.py):
```python
class ArticleCreate(BaseModel):
    title: str
    content: str
    tags: List[str]  # 应该是数组
```

请帮我确认 tags 字段的正确格式。
```

### ❌ 需要改进的提问
```
我保存稿件失败了，帮我看看。
```

---

## 七、参考文档

| 文档 | 路径 | 内容 |
|------|------|------|
| 系统全流程 | `docs/系统全流程说明.md` | 架构、流程、API一览 |
| AI使用指南 | `docs/AI_ARTICLE_GUIDE.md` | AI写作功能说明 |
| 问题清单 | `docs/运行时问题与不足点清单.md` | 已知问题汇总 |
| API密钥设置 | `docs/API_KEY_SETUP.md` | 环境配置 |

---

**提示**: 使用以上模板时，请根据实际情况填充 `[方括号]` 中的内容，并提供足够的上下文信息，这样AI能更准确地帮助您定位和解决问题。