# 新闻线索采集系统修复 - AI 提示词

## 提示词正文（可直接复制使用）

```
【角色定位】
你是新闻采编系统的后端开发专家，精通 Python FastAPI、网络爬虫、RSS 解析和 API 集成。

【任务目标】
修复并优化新闻线索采集系统，确保大部分信源能够成功采集，提升整体采集成功率和数据质量。

【项目背景】
这是一个基于 FastAPI 的新闻内容采编系统，线索采集功能位于：
- 主文件: backend/app/routers/clues.py (1500+ 行，包含完整采集逻辑)
- 配置: backend/app/core/config.py (爬虫超时、User-Agent 等)
- 模型: backend/app/models/clue.py (Clue 数据模型)

【当前系统架构】

1. **信源分类** (已配置但未完全验证可用性):
   - **RSS 源** (10个): IT之家、36氪、少数派、钛媒体、开源中国、奇客Solidot、掘金、InfoQ中文、爱范儿、极客公园
   - **API/JSON 源** (4个): 知乎日报、百度热搜、今日头条、B站热门
   - **搜索源** (10个): 腾讯新闻、网易新闻、新浪新闻、凤凰网、搜狐新闻、今日头条、微博、知乎、政府网站、社交媒体综合

2. **核心采集函数**:
   - `_fetch_rss_feed()`: RSS 源采集，支持 XML 和 HTML 回退解析
   - `_fetch_api_feed()`: API/JSON 源采集，支持多种数据格式
   - `_fetch_bing_news()`: Bing 新闻搜索采集
   - `_fetch_web_results()`: 统一采集入口，智能路由
   - `collect_multichannel()`: 多渠道并发采集 API 端点

3. **采集流程**:
   - 前端调用 POST/GET /api/clues/collect/multichannel
   - 支持多关键词、多渠道并发采集 (ThreadPoolExecutor)
   - 自动去重、评分、存入数据库

【当前已知问题】
1. 部分 RSS 源可能返回非标准格式 (如 36氪有反爬机制)
2. 部分 API 源可能需要更新请求头或解析逻辑
3. 某些信源可能已失效或变更接口
4. 超时和重试机制可能需要优化
5. 缺少对采集失败的详细日志和统计

【修复要求】

1. **信源可用性检测与修复**:
   - 验证所有 RSS 源是否可访问，修复失效的解析逻辑
   - 验证所有 API 源的数据格式，更新变更的接口
   - 对不可用的信源提供替代方案或标记为失效

2. **采集成功率提升**:
   - 优化请求头配置 (Referer、Accept、Cookie 等)
   - 实现智能重试机制 (指数退避、切换 User-Agent)
   - 增强反爬绕过能力 (请求间隔、随机延迟)

3. **错误处理与监控**:
   - 完善异常捕获，区分网络错误、解析错误、反爬拦截
   - 添加详细的采集日志 (成功/失败/重试次数)
   - 实现信源健康状态检查接口

4. **代码结构优化**:
   - 将采集逻辑模块化，便于维护和扩展
   - 统一错误处理和响应格式
   - 添加类型注解和文档注释

5. **新增信源建议** (可选):
   - 补充稳定可靠的新闻源
   - 考虑添加缓存机制减少重复请求

【技术约束】
- 使用现有的 requests + BeautifulSoup 技术栈
- 保持与现有数据库模型兼容
- 不要引入重量级依赖 (如 Scrapy)
- 遵守网站的 robots.txt 和访问频率限制
- 保持配置化设计，信源信息可动态调整

【输出要求】
1. 提供修复后的完整代码文件 (clues.py 或拆分后的模块)
2. 提供信源可用性测试报告 (哪些可用、哪些需要调整)
3. 提供配置说明 (如需调整环境变量或添加新信源)
4. 提供测试命令或脚本，验证采集功能

【验收标准】
- 至少 70% 的 RSS 源能够正常采集
- 至少 80% 的 API 源能够正常采集
- 采集成功率 > 80% (成功采集数/尝试采集数)
- 系统能够优雅处理失败情况，不影响其他信源
- 代码通过类型检查，无明显的语法或逻辑错误

请基于以上要求，分析现有代码并给出修复方案。如果需要，可以先输出诊断报告，再输出修复代码。
```

---

## 使用说明

### 何时使用此提示词

1. **线索采集功能失效** - 前端点击采集无结果或报错
2. **信源返回空数据** - 部分或全部信源采集不到内容
3. **需要新增信源** - 扩展更多的新闻来源
4. **采集成功率低** - 大量请求失败或超时

### 配合使用的诊断命令

在提交提示词前，可以先运行以下命令收集诊断信息：

```bash
# 1. 测试后端服务是否正常运行
curl http://localhost:8000/api/health

# 2. 获取信源列表
curl http://localhost:8000/api/clues/sources

# 3. 测试单个信源采集 (替换为实际的信源key)
curl "http://localhost:8000/api/clues/collect/multichannel?keywords=科技&channels=ithome&max_results=5"

# 4. 测试多渠道采集
curl "http://localhost:8000/api/clues/collect/multichannel?keywords=人工智能&channels=ithome,36kr,zhihu_daily&max_results=10"

# 5. 验证信源可访问性
curl -X POST http://localhost:8000/api/clues/sources/verify \
  -H "Content-Type: application/json" \
  -d '{"url": "https://www.ithome.com/rss/", "type": "rss", "key": "ithome"}'
```

### 需要提供给 AI 的上下文文件

为了获得最佳修复效果，建议将以下文件内容一并提供给 AI：

1. **必须提供**:
   - `backend/app/routers/clues.py` (完整采集逻辑)
   - `backend/app/core/config.py` (爬虫配置)

2. **建议提供**:
   - `backend/app/models/clue.py` (数据模型)
   - 后端日志输出 (运行采集时的日志)

### 预期修复内容

AI 应该提供以下输出：

1. **诊断报告** (可选):
   ```markdown
   ## 信源可用性诊断
   
   | 信源 | 类型 | 状态 | 问题描述 | 修复建议 |
   |------|------|------|----------|----------|
   | ithome | RSS | ✅ 正常 | - | 无需修复 |
   | 36kr | RSS | ⚠️ 异常 | 返回 HTML 而非 RSS | 更新解析逻辑 |
   | ... | ... | ... | ... | ... |
   ```

2. **修复后的代码**:
   - 完整的 `clues.py` 文件
   - 或拆分后的模块文件 (如 `collectors/rss_collector.py`, `collectors/api_collector.py`)

3. **配置更新说明**:
   ```markdown
   ## 配置变更
   
   1. 在 `.env` 文件中添加:
      CRAWLER_TIMEOUT=30
      CRAWLER_RETRY_TIMES=3
   
   2. 如需添加新信源，在 `clues.py` 的 `_RSS_FEEDS` 或 `_API_FEEDS` 中添加配置
   ```

4. **测试验证**:
   ```bash
   # 运行测试命令验证修复效果
   curl "http://localhost:8000/api/clues/collect/multichannel?keywords=测试&max_results=20"
   ```

---

## 进阶：分阶段修复提示词

如果问题复杂，可以分阶段提交：

### 阶段一：诊断分析
```
请分析 backend/app/routers/clues.py 中的线索采集系统，输出：
1. 当前配置的所有信源列表
2. 每个信源的采集逻辑和潜在问题
3. 建议的修复优先级排序
```

### 阶段二：核心修复
```
基于诊断报告，修复以下高优先级问题：
1. [具体问题1]
2. [具体问题2]
...

请提供修复后的代码和测试方法。
```

### 阶段三：优化增强
```
在核心功能修复的基础上，添加以下增强功能：
1. 信源健康检查接口
2. 采集成功率统计
3. 智能重试机制
```

---

## 常见问题快速修复参考

| 问题现象 | 可能原因 | 快速修复方向 |
|----------|----------|--------------|
| 所有 RSS 源返回空 | 解析逻辑错误 | 检查 XML 解析和 HTML 回退逻辑 |
| 特定源 403/503 | 反爬拦截 | 更新 User-Agent、添加 Referer |
| API 返回格式错误 | 接口变更 | 更新 JSON 解析路径 |
| 采集超时 | 网络慢或源失效 | 调整超时时间、添加快速失败 |
| 重复数据多 | 去重逻辑问题 | 检查 seen_keys 集合逻辑 |

---

**提示**: 使用此提示词时，请确保提供完整的 `clues.py` 文件内容，这样 AI 才能基于实际代码进行精准修复。