# 基于大模型的新闻内容采编系统总架构说明

## 1. 项目定位

本项目是一个面向新闻采编业务的智能化系统，核心目标是把新闻生产中的“线索采集、选题策划、AI 稿件生成、人工审核、发布展示、读者反馈”串成完整流程。

系统主要服务对象包括：

- 新闻采编人员：快速发现线索、整理素材、生成初稿、管理稿件。
- 审核人员：集中查看待审稿件，结合 AI 预审结果辅助判断。
- 管理员：查看系统中的管理员、审核员、记者、编辑、投稿用户等人员情况。
- 普通读者：浏览已发布新闻，进行点赞、评论、阅读互动。

项目重点解决的问题：

- 线索来源分散，人工收集成本高。
- 稿件初稿、标题、摘要、关键词等重复劳动多。
- 审核过程缺少统一记录，稿件状态流转不清晰。
- 传统 CMS 通常只负责内容管理，缺少从线索到 AI 写稿再到审核发布的闭环。

## 2. 总体架构

系统采用前后端分离架构，前端负责页面交互和业务操作，后端提供 REST API，数据库负责持久化存储，大模型服务负责 AI 生成、润色和预审。

```text
用户浏览器
  |
  |  React + TypeScript + Ant Design
  v
前端应用 frontend
  |
  |  Axios / REST API / JWT Token
  v
FastAPI 后端 backend
  |
  |  SQLAlchemy ORM
  v
MySQL 数据库

FastAPI 后端
  |
  |  OpenAI 兼容协议
  v
DeepSeek / SiliconFlow / OpenAI 等大模型服务
```

### 架构特点

- 前后端分离：前端和后端通过 API 通信，职责清晰。
- 分角色页面：采编端、审核端、读者端、管理员查看页分开设计。
- 数据统一入库：线索、稿件、审核记录、用户、反馈数据都存入 MySQL。
- AI 能力服务化：后端统一封装 LLM 调用，便于切换 DeepSeek、SiliconFlow、OpenAI 等模型。
- 启动脚本统一入口：本地主要通过 `./start.sh` 启动项目。

## 3. 技术选型

| 层级 | 技术 | 作用 |
|------|------|------|
| 前端框架 | React 18 | 构建单页应用和组件化页面 |
| 前端语言 | TypeScript | 增强类型约束，减少接口字段误用 |
| UI 组件库 | Ant Design 5 | 快速构建表格、表单、弹窗、抽屉、统计卡片等后台系统界面 |
| 构建工具 | Vite | 提供快速开发服务器和前端打包能力 |
| 请求库 | Axios | 统一封装接口请求、Token、错误处理 |
| 后端框架 | FastAPI | 提供高性能 API、自动接口文档、Pydantic 数据校验 |
| ORM | SQLAlchemy 2.0 | 管理数据模型和数据库操作 |
| 数据校验 | Pydantic v2 | 定义请求体和响应数据结构 |
| 数据库 | MySQL 8.0 | 保存用户、线索、稿件、审核、反馈等业务数据 |
| 数据库驱动 | PyMySQL | Python 连接 MySQL |
| 认证 | JWT | 登录后通过 Token 鉴权 |
| AI 调用 | OpenAI SDK 兼容协议 | 统一调用 DeepSeek、SiliconFlow、OpenAI 等模型 |
| 网页采集 | curl_cffi + BeautifulSoup | 模拟浏览器请求并解析 RSS、JSON、HTML 搜索结果 |

## 4. 为什么选择 React

本项目选择 React，主要是因为系统前端属于典型的管理型、交互型应用，需要大量表格、筛选、表单、弹窗、状态切换和局部刷新。

选择 React 的原因：

- 组件化能力强：线索列表、稿件编辑、审核队列、用户查看等页面都能拆成独立组件。
- 生态成熟：React Router、Axios、Ant Design 等配套完善。
- 状态驱动 UI：用户选择线索、切换审核状态、打开详情抽屉时，界面能根据状态自动刷新。
- 适合后台系统：配合 Ant Design 可以快速实现规范统一的管理端界面。
- TypeScript 友好：接口数据字段较多，使用 TypeScript 能减少前后端字段不一致造成的问题。

可以在答辩中这样回答：

> 我选择 React 是因为本系统不是静态展示页面，而是一个多角色、多状态、多表单的采编管理系统。React 的组件化和状态管理方式更适合构建这类复杂交互页面，同时 Ant Design 能快速提供表格、表单、抽屉、弹窗等成熟组件，提高开发效率和界面一致性。

## 5. 后端架构

后端基于 FastAPI，主要目录如下：

```text
backend/
├── app/
│   ├── main.py              # FastAPI 应用入口，注册中间件和路由
│   ├── database.py          # SQLAlchemy Engine、Session
│   ├── core/
│   │   └── config.py        # 数据库、AI、JWT、CORS 等配置
│   ├── models/              # SQLAlchemy 数据模型
│   ├── routers/             # API 路由
│   ├── schemas/             # Pydantic 请求响应模型
│   ├── services/            # AI、缓存、限流等服务封装
│   └── migrations/          # 表结构补丁与初始化逻辑
├── requirements.txt
└── init.sql
```

主要 API 模块：

| 模块 | 路由前缀 | 说明 |
|------|----------|------|
| 认证 | `/api/auth` | 登录、注册、当前用户信息 |
| 线索 | `/api/clues` | 线索列表、采集、搜索资料、AI 分析 |
| 选题 | `/api/topics` | 选题创建、状态管理、稿件关联 |
| 稿件 | `/api/articles` | 稿件增删改查、提交审核、发布 |
| 内容生成 | `/api/content` | AI 写稿、标题生成、摘要、关键词、AI 预审 |
| 审核 | `/api/reviews` | 待审队列、审核记录、审核结果提交 |
| 反馈 | `/api/feedback` | 阅读、点赞、评论等反馈统计 |
| 公开访问 | `/api/public` | 读者端文章列表和详情 |
| 管理员 | `/api/users/admin/users` | 查看用户列表和角色数据 |

## 6. 前端架构

前端基于 React 18、TypeScript、Vite 和 Ant Design。

```text
frontend/
├── src/
│   ├── App.tsx              # 前端路由入口
│   ├── pages/
│   │   ├── editor/          # 采编端页面
│   │   ├── review/          # 审核端页面
│   │   ├── reader/          # 读者端页面
│   │   └── auth/            # 登录注册
│   ├── services/
│   │   ├── api.ts           # Axios API 封装
│   │   └── authClient.ts    # 登录、Token、角色跳转
│   ├── components/          # 公共组件
│   ├── utils/               # 工具函数
│   └── constants/           # 路由、常量
├── package.json
└── vite.config.ts
```

前端页面按角色拆分：

| 页面 | 说明 |
|------|------|
| 采编端首页 | 展示线索、稿件、待审、发布等概览 |
| 新闻线索 | 采集线索、搜索资料、AI 分析、查看详情 |
| AI 稿件生成 | 选择线索、输入主题/关键词/提示词，调用 AI 生成稿件 |
| 稿件管理 | 创建、编辑、提交审核、查看审核记录 |
| 审核端 | 查看待审稿件、AI 预审分、提交审核意见 |
| 读者端 | 浏览已发布文章、搜索、点赞、评论 |
| 管理员端 | 按角色查看用户和人员业务数据 |

## 7. 数据库设计

当前项目使用 MySQL 作为主要数据库，通过 SQLAlchemy ORM 操作。

核心数据表：

| 表 | 作用 |
|----|------|
| `users` | 用户账号、角色、邮箱、启用状态 |
| `clues` | 新闻线索、来源、正文摘要、关键词、评分、创建者 |
| `topics` | 选题策划、负责人、状态、AI 评分 |
| `articles` | 新闻稿件、正文、摘要、分类、状态、作者 |
| `reviews` | 审核记录、审核人、审核结果、审核意见 |
| `feedback` / `feedbacks` | 阅读量、点赞量、评论量、热度数据 |
| `messages` | 系统消息和用户消息 |
| `collections` | 采集任务和采集状态 |

数据库连接配置在 `backend/app/database.py`，如果 `DATABASE_URL` 是 MySQL 地址，则使用连接池：

- `pool_size=10`
- `max_overflow=20`
- `pool_pre_ping=True`
- `pool_recycle=3600`

这样可以减少频繁创建连接的开销，并避免 MySQL 空闲连接断开导致接口失败。

## 8. 核心业务流程

### 8.1 新闻生产流程

```text
新闻线索采集
  ↓
线索入库并计算基础评分
  ↓
编辑选择线索或手动输入主题
  ↓
AI 生成标题、正文、摘要和写作角度
  ↓
保存为稿件草稿
  ↓
提交审核
  ↓
审核员查看 AI 预审分和稿件详情
  ↓
人工审核通过 / 退回 / 拒绝
  ↓
发布到读者端
  ↓
读者浏览、点赞、评论，形成反馈数据
```

### 8.2 稿件状态流转

```text
draft 草稿
  → pending_review 待审核
  → reviewing 审核中
  → published 已发布

异常状态：
pending_review / reviewing
  → rejected 已拒绝
  → draft 退回修改
```

## 9. AI 能力实现

### 9.1 AI 调用架构

后端通过 `backend/app/services/llm.py` 统一封装大模型调用。业务代码不直接绑定某一个模型，而是通过 `LLMFactory.create()` 创建统一客户端。

支持的 Provider 包括：

- DeepSeek
- SiliconFlow
- OpenAI
- OpenRouter
- Moonshot / Kimi
- Ollama 本地模型

当前项目主要通过 OpenAI 兼容协议调用大模型，配置项包括：

- `DEEPSEEK_API_KEY`
- `DEEPSEEK_BASE_URL`
- `SILICONFLOW_API_KEY`
- `SILICONFLOW_BASE_URL`
- `OPENAI_API_KEY`
- `AI_MODEL`
- `AI_TIMEOUT`

注意：答辩时不要展示真实 API Key，只说明通过 `.env` 配置，GitHub 只提交 `.env.example`。

### 9.2 AI 稿件生成怎么做

AI 写稿接口是：

```text
POST /api/content/generate
```

前端页面是：

```text
frontend/src/pages/editor/AIArticle.tsx
```

发送给后端的数据包括：

- `topic`：主题或标题
- `keywords`：关键词
- `style`：文章风格
- `length`：文章长度
- `title_count`：备选标题数量
- `tone`：语调
- `audience`：目标受众
- `reference_material`：参考素材
- `custom_prompt`：用户额外提示词

后端会把这些字段拼成提示词，再调用大模型。大模型需要返回 JSON：

```json
{
  "suggested_titles": ["标题1", "标题2", "标题3"],
  "draft_content": "完整稿件正文",
  "summary": "摘要",
  "recommended_angles": ["角度1", "角度2"],
  "structure_suggestion": "结构建议"
}
```

### 9.3 新增的“输入提示词对话框”有什么作用

AI 稿件生成页面已经支持额外提示词。用户可以在“AI 提示词（可选）”中输入自己的写作要求，例如：

- 希望文章更正式
- 希望突出某个角度
- 希望控制段落结构
- 希望避免夸张标题
- 希望更适合校园媒体或政务新闻表达

这些内容会作为 `custom_prompt` 字段传给后端，后端再追加到最终 prompt 中，与主题、关键词、参考素材一起发给 AI。

答辩时可以这样说：

> 系统不是固定模板式生成，而是允许用户在生成前输入额外提示词。这个提示词会和线索内容、关键词、风格、长度等参数一起发送给大模型，因此用户可以控制写作方向、语气和注意事项。

## 10. 三个重点问题回答

### 问题 1：目前 AI 审核是如何审核的？是不是 mock？

当前项目的 AI 审核分两层：

第一层是前端快速展示的本地备用评分。

在审核端页面中，系统会先根据稿件正文长度给出一个临时分数：

- 正文长度大于 500：约 85 分，建议通过。
- 正文长度大于 200：约 70 分，建议人工复核。
- 正文更短：约 55 分，建议不通过。

这个逻辑主要是为了让页面快速显示，不等 AI 接口返回。它属于离线兜底，不是最终 AI 审核。

第二层是后端真实 AI 预审。

前端随后会调用：

```text
POST /api/content/ai-review
```

后端会根据 `article_id` 查询稿件标题、摘要、正文，然后构造审核提示词，让大模型从四个维度判断：

- 事实准确性：是否有明显事实错误。
- 语言规范：语法、用词是否规范。
- 结构逻辑：文章结构是否清晰合理。
- 新闻价值：选题是否有新闻价值。

AI 需要返回 JSON：

```json
{
  "result": "pass/reject/revision",
  "score": 80,
  "comment": "审核意见",
  "issues": ["问题1"],
  "suggestions": ["建议1"]
}
```

如果 AI 服务没有配置，后端会返回默认通过和 80 分；如果 AI 调用失败，前端保留本地备用评分。所以更准确的说法是：

> 项目已经实现了 AI 预审接口，会调用大模型进行内容审核；同时为了保证系统可演示和可用，保留了基于字数的前端兜底评分和后端默认结果。最终发布仍由人工审核员确认，AI 的定位是辅助审核，不是完全替代人工。

### 问题 2：AI 稿件生成是如何基于线索生成的？如果是视频怎么办？如果是网页怎么办？

AI 稿件生成的核心输入是“主题 + 关键词 + 参考素材 + 用户提示词”。

当用户在“从线索生成”中选择线索时，前端会做三件事：

- 把所选线索的 `content` 合并成 `reference_material`。
- 把所选线索的关键词合并去重后填入关键词。
- 把线索标题拼成一个“建议主题”，用户可以手动采纳。

然后前端调用 `POST /api/content/generate`，后端把参考素材和提示词一起发给大模型生成稿件。

如果线索来自视频，例如 B 站热门，当前系统采集到的是：

- 视频标题
- 视频链接
- 播放量、弹幕量等基础信息，作为 snippet
- 来源名称，例如 B站热门

当前没有做视频语音转文字，也没有下载视频内容。因此视频类线索生成稿件时，主要依据是标题、链接、播放/弹幕等摘要信息，以及用户额外补充的提示词。如果需要更完整的视频稿件生成，后续可以扩展 ASR 语音识别，把视频字幕或音频转写结果作为 `reference_material`。

如果线索来自网页或搜索结果，当前系统主要提取：

- 搜索结果标题
- URL
- 搜索摘要 snippet
- 来源名称

AI 生成时使用的是已经入库到线索 `content` 字段里的摘要文本。系统目前不是完整正文抓取器，不会对所有网页都深度阅读全文；它更偏向“线索发现 + 摘要辅助生成”。如果答辩被问到，可以这样回答：

> 当前版本为了保证稳定性，主要使用标题、摘要、来源链接等结构化线索作为生成依据。对于网页，系统采集搜索结果摘要和 RSS/API 摘要；对于视频，使用标题和基础热度信息。后续可以扩展正文抽取、视频字幕提取和语音转写，将更完整的原始内容喂给 AI。

### 问题 3：现在爬取的是新闻线索，如何确保把它变成文字喂给 AI？

线索采集接口是：

```text
GET/POST /api/clues/collect/multichannel
```

采集流程如下：

```text
输入关键词
  ↓
选择采集渠道
  ↓
从 RSS / JSON API / Bing 搜索获取结果
  ↓
解析 title、url、snippet、source、category
  ↓
把 snippet 或 title 写入 clues.content
  ↓
用户在线索页面选择线索
  ↓
前端把线索 content 合并成 reference_material
  ↓
发送给 /api/content/generate
  ↓
AI 根据文字素材生成稿件
```

也就是说，系统不是把网页或视频原文件直接发给 AI，而是先把采集结果整理成文字字段：

- `title`：线索标题
- `content`：线索摘要或标题
- `source`：来源
- `source_url`：来源链接
- `keywords`：关键词

这些文字字段入库后，再作为 AI 写稿的参考素材。

答辩时可以这样说：

> 我把外部来源采集结果先统一转换成线索数据模型，也就是标题、摘要、来源、链接和关键词。AI 不直接处理杂乱网页，而是处理系统清洗后的文字字段。这样可以减少网页结构变化对 AI 生成的影响，也便于后续人工编辑校对。

## 11. 线索采集实现

线索采集主要在：

```text
backend/app/routers/clues.py
```

当前保留的稳定信源包括：

- 腾讯新闻
- 新浪新闻
- 网易新闻入口
- 知乎日报
- B站热门
- Bing 搜索
- Bing 社交定向搜索
- Bing 政府网站定向搜索

采集方式包括：

- RSS / XML 解析
- JSON API 解析
- HTML 搜索结果解析
- Bing RSS 兜底

采集时会使用：

- `curl_cffi` 模拟浏览器请求，降低被反爬拦截概率。
- `BeautifulSoup` 解析 HTML。
- `xml.etree.ElementTree` 解析 RSS/XML。
- 关键词匹配和相关度排序，提高结果相关性。

当前版本已经按用户隔离线索数据：

- 管理员可以查看全站线索。
- 普通账号只能查看自己创建或采集的线索。
- 线索入库时保存 `creator_id`，避免一个人的线索出现在另一个人的账号中。

## 12. 审核流程实现

审核流程由稿件状态和审核记录共同完成。

用户在稿件管理中提交审核后：

- 稿件状态变为 `pending_review`。
- 审核端可以看到待审稿件。
- AI 审核页先显示本地兜底评分。
- 后台批量调用 AI 预审接口，返回后替换评分和建议。
- 审核员可以通过、拒绝或退回修改。

审核结果写入 `reviews` 表，并同步更新 `articles.status`。

需要注意：

> AI 审核不是最终自动发布机制，最终仍然通过人工审核操作完成。这样更符合新闻采编中“AI 辅助、人负责”的设计原则。

## 13. 管理员端实现

管理员端目前按用户角色分成多个页面标签：

- 全部用户
- 管理员
- 审核员
- 记者
- 编辑
- 投稿用户

页面只保留查看功能，不提供删除、系统配置、统计大屏等复杂操作，避免答辩演示时页面过于集中。

管理员可以查看：

- 用户姓名 / 昵称 / 账号
- 角色
- 邮箱
- 启用状态
- 加入时间
- 稿件数、发布数、待审核数、草稿数、线索数、审核数

详细数据放在右侧详情抽屉中展示，列表保持简洁。

## 14. 安全与配置

项目中不能提交真实密钥。GitHub 展示时应只提交：

- `.env.example`
- `backend/.env.example`
- `frontend/.env.example`

真实配置放在本地 `.env` 中，不上传仓库。

重要配置包括：

- `DATABASE_URL`
- `SECRET_KEY`
- `DEEPSEEK_API_KEY`
- `SILICONFLOW_API_KEY`
- `OPENAI_API_KEY`
- `AI_MODEL`
- `CORS_ORIGINS`

答辩或 GitHub 展示时需要注意：

- 不展示真实 API Key。
- 不展示本地数据库密码。
- 不把上传文件、构建产物、依赖目录提交到仓库。
- 说明 `.gitignore` 已排除 `.env`、`node_modules`、`dist`、缓存文件等。

## 15. 运行方式

本地运行主要使用：

```bash
./start.sh
```

脚本负责：

- 检查 Python 环境。
- 检查 Node 环境。
- 检查 MySQL 状态。
- 初始化或迁移数据库表。
- 启动 FastAPI 后端。
- 启动 Vite 前端。

前端默认访问：

```text
http://localhost:3000
```

后端默认访问：

```text
http://localhost:8000
```

Swagger 接口文档：

```text
http://localhost:8000/docs
```

## 16. 答辩时需要注意的回答点

### 16.1 不要把 AI 审核说成完全自动审核

推荐说法：

> 系统实现了 AI 预审能力，会对稿件质量进行辅助评分和建议，但最终审核结果仍由审核员确认。这样可以提高审核效率，同时保留人工把关。

### 16.2 不要说系统已经完整解析所有网页正文和视频内容

推荐说法：

> 当前版本主要采集线索级信息，包括标题、摘要、来源链接和热度数据。网页正文抽取、视频字幕识别属于后续可扩展方向。

### 16.3 说明为什么需要 MySQL

推荐说法：

> 新闻采编系统有用户、线索、稿件、审核、反馈等多类结构化数据，并且需要状态流转和关联查询，所以使用 MySQL 做持久化存储，比只用本地文件更可靠。

### 16.4 说明为什么后端用 FastAPI

推荐说法：

> FastAPI 性能较好，接口开发效率高，支持 Pydantic 自动校验和 Swagger 文档，非常适合本项目这种前后端分离、接口较多的系统。

### 16.5 说明创新点

可以从三个角度讲：

- 流程创新：不是单独的 AI 写作工具，而是覆盖线索、写稿、审核、发布、反馈的完整采编闭环。
- 交互创新：AI 生成时支持额外提示词，用户可以主动控制稿件方向。
- 业务创新：将新闻线索采集与 AI 稿件生成结合，降低从素材发现到初稿形成的时间成本。

## 17. 可以继续优化的方向

后续可以扩展：

- 网页正文深度抽取。
- 视频字幕识别或语音转文字。
- AI 审核维度细化，例如敏感词、事实核查、版权风险。
- 多级审核流程，例如初审、复审、终审。
- AI 生成结果溯源，把参考素材和生成段落建立对应关系。
- 更细的数据看板，例如不同记者产出、稿件通过率、线索转化率。

