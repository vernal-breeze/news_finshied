# 架构文档

## 一、总体架构

系统采用前后端分离架构，由 React 前端、FastAPI 后端、MySQL 数据库和大模型服务组成。

```text
┌─────────────────────────────────────────────┐
│ 前端 React + Vite + Ant Design              │
│ /editor 采编端  /review 审核端  /reader 读者端 │
└─────────────────────┬───────────────────────┘
                      │ REST API
┌─────────────────────▼───────────────────────┐
│ 后端 FastAPI                                  │
│ 认证 / 线索 / 选题 / 稿件 / AI生成 / 审核 / 反馈 │
└─────────────────────┬───────────────────────┘
                      │ SQLAlchemy ORM
┌─────────────────────▼───────────────────────┐
│ MySQL 8.0                                    │
│ users / clues / topics / articles / reviews │
└─────────────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────┐
│ SiliconFlow DeepSeek-V3                      │
│ 线索分析 / 稿件生成 / 内容润色 / 审核预审      │
└─────────────────────────────────────────────┘
```

## 二、前端架构

前端基于 React 18、TypeScript、Vite 和 Ant Design 实现，按照业务端拆分页面。

| 模块 | 路径 | 说明 |
|------|------|------|
| 采编端 | `frontend/src/pages/editor/` | 首页、线索、选题、AI 写作、稿件、统计 |
| 审核端 | `frontend/src/pages/review/` | 待审队列、审核详情、发布管理 |
| 读者端 | `frontend/src/pages/reader/` | 文章列表、文章详情、评论、消息 |
| 认证 | `frontend/src/pages/auth/` | 登录、注册 |
| API 层 | `frontend/src/services/` | 请求封装、Token 处理、接口适配 |

路由入口位于 `frontend/src/App.tsx`，统一处理采编端、审核端、读者端和登录注册页面。

## 三、后端架构

后端基于 FastAPI，按路由、模型、服务、Schema 分层。

| 层级 | 路径 | 说明 |
|------|------|------|
| 应用入口 | `backend/app/main.py` | 创建 FastAPI 实例、注册中间件和路由 |
| 配置 | `backend/app/core/config.py` | 数据库、JWT、AI、CORS 等配置 |
| 数据库 | `backend/app/database.py` | SQLAlchemy Engine、Session 和依赖注入 |
| 模型 | `backend/app/models/` | 用户、线索、选题、稿件、审核、反馈等实体 |
| 路由 | `backend/app/routers/` | REST API 接口 |
| 服务 | `backend/app/services/` | AI、稿件、线索等业务逻辑 |
| Schema | `backend/app/schemas/` | 请求和响应数据结构 |

主要 API 模块：

| 模块 | 接口前缀 | 说明 |
|------|----------|------|
| 认证 | `/api/auth` | 登录、注册、当前用户 |
| 线索 | `/api/clues` | 线索列表、采集、AI 分析 |
| 选题 | `/api/topics` | 选题管理 |
| 稿件 | `/api/articles` | 稿件 CRUD、提交审核、发布 |
| 内容生成 | `/api/content` | AI 稿件生成、标题摘要关键词生成 |
| 审核 | `/api/reviews` | 审核队列、审核记录 |
| 反馈 | `/api/feedback` | 浏览、点赞、评论统计 |
| 读者端 | `/api/public` | 公开文章、评论、互动 |

## 四、业务流程

### 1. 新闻生产流程

```text
线索采集
  ↓
AI 分析新闻价值
  ↓
创建选题
  ↓
AI 辅助生成稿件
  ↓
编辑修改
  ↓
提交审核
  ↓
审核通过
  ↓
发布到读者端
  ↓
读者反馈统计
```

### 2. 稿件状态流转

```text
draft 草稿
  → pending_review 待审核
  → reviewing 审核中
  → approved 已通过
  → published 已发布
```

异常流转：

```text
pending_review / reviewing
  → rejected 已拒绝
  → draft 退回修改
```

## 五、数据库设计

系统核心数据表共 8 类：

| 表 | 主要作用 |
|----|----------|
| `users` | 用户账号、角色、状态 |
| `clues` | 新闻线索、来源、分类、AI 评分 |
| `topics` | 选题信息、负责人、状态 |
| `articles` | 标题、正文、摘要、作者、状态、发布时间 |
| `reviews` | 审核层级、审核结果、审核意见 |
| `feedbacks` | 浏览量、点赞量、评论量等反馈数据 |
| `messages` | 系统消息和用户消息 |
| `collections` | 采集任务与采集状态 |

## 六、部署架构

本地开发推荐使用 `start.sh`，脚本负责检查环境、启动 MySQL、初始化表结构，并启动后端和前端。

Docker 部署使用 `docker-compose.yml`，包括：

- MySQL 服务
- FastAPI 后端服务
- React 前端构建与静态服务

项目使用 PyMySQL 连接 MySQL，避免 `mysqlclient` 在 Apple Silicon 环境下的编译问题。

