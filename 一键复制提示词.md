# 新闻线索采集系统修复 - 一键复制提示词

> 复制下方代码块中的全部内容，粘贴给 AI 即可。

---

```
【角色定位】
你是新闻采编系统的后端开发专家，精通 Python FastAPI、网络爬虫、RSS 解析和 API 集成。

【任务目标】
修复并优化新闻线索采集系统，确保大部分信源能够成功采集，提升整体采集成功率和数据质量。

【项目背景】
这是一个基于 FastAPI 的新闻内容采编系统，线索采集功能位于：
- 主文件: backend/app/routers/clues.py (1500+ 行，包含完整采集逻辑)
- 配置: backend/app/core/config.py (爬虫超时、User-Agent 等)
- 模型: backend/app/models/clue.py (Clue 数据模型)

【当前系统架构】

1. 信源分类 (已配置但未完全验证可用性):
   - RSS源(10个): IT之家、36氪、少数派、钛媒体、开源中国、奇客Solidot、掘金、InfoQ中文、爱范儿、极客公园
   - API/JSON源(4个): 知乎日报、百度热搜、今日头条、B站热门
   - 搜索源(10个): 腾讯新闻、网易新闻、新浪新闻、凤凰网、搜狐新闻、今日头条、微博、知乎、政府网站、社交媒体综合

2. 核心采集函数:
   - _fetch_rss_feed(): RSS源采集，支持XML和HTML回退解析
   - _fetch_api_feed(): API/JSON源采集，支持多种数据格式
   - _fetch_bing_news(): Bing新闻搜索采集
   - _fetch_web_results(): 统一采集入口，智能路由
   - collect_multichannel(): 多渠道并发采集API端点

3. 采集流程:
   - 前端调用 POST/GET /api/clues/collect/multichannel
   - 支持多关键词、多渠道并发采集 (ThreadPoolExecutor)
   - 自动去重、评分、存入数据库

【当前已知问题】
1. 部分RSS源返回非标准格式 (如36氪有反爬机制)
2. 部分API源需要更新请求头或解析逻辑
3. 某些信源已失效或变更接口
4. 超时和重试机制需优化
5. 采集失败缺少详细日志和统计
6. 搜索结果与关键词不匹配，精确度低
7. 第二次搜索返回大量重复数据，去重失效
8. toutiao分支存在变量未初始化bug: "cannot access local variable 'results' where it is not associated with a value"
9. /api/stats/dashboard 和 /api/feedback/stats 返回500

【修复要求】

1. 信源可用性检测与修复:
   - 验证所有RSS源是否可访问，修复失效的解析逻辑
   - 验证所有API源的数据格式，更新变更的接口
   - 不可用的源直接删除，只保留验证可用的（约10个）
   - 优先保留稳定性高的源（IT之家、知乎日报、B站热门等），RSS和API源各保留一部分

2. 采集成功率提升:
   - 优化请求头配置 (Referer、Accept、Cookie 等)
   - 实现智能重试机制 (指数退避、切换 User-Agent)
   - 增强反爬绕过能力 (请求间隔、随机延迟)

3. 搜索精确度提升:
   - 所有搜索结果必须通过关键词匹配过滤（标题或snippet包含关键词）
   - 中文关键词支持核心词匹配（拆分2字以上词组，任一命中即可）
   - 结果按匹配度排序（标题完全包含 > 部分包含 > 仅snippet包含）

4. 去重机制修复:
   - 入库前查询数据库，按 title+source 去重
   - 避免多次搜索返回重复线索

5. 错误处理与监控:
   - 完善异常捕获，区分网络错误、解析错误、反爬拦截
   - 添加详细的采集日志 (成功/失败/重试次数)
   - 实现信源健康状态检查接口

6. 新增搜索源 (3-4个):
   - 搜狗新闻搜索 (news.sogou.com)
   - 头条搜索 (so.toutiao.com)
   - 微信搜一搜 (weixin.sogou.com)
   - 必应国际版新闻 (cn.bing.com/news)
   - 每个源写独立的 _fetch_xxx_news() 函数
   - 超时8秒，失败静默跳过，不影响其他源

7. 代码结构优化:
   - 将采集逻辑模块化，便于维护和扩展
   - 统一错误处理和响应格式
   - 添加类型注解和文档注释

8. 500错误修复:
   - 修复 clues.py 中 toutiao 分支的 results 变量未初始化bug
   - 检查 stats.py 的 /api/stats/dashboard 接口
   - 检查 feedback.py 的 /api/feedback/stats 接口

【技术约束】
- 使用现有的 requests + BeautifulSoup 技术栈
- 保持与现有数据库模型兼容
- 不要引入重量级依赖 (如 Scrapy)
- 遵守网站的 robots.txt 和访问频率限制
- 保持配置化设计，信源信息可动态调整

【输出要求】
1. 先输出信源可用性诊断报告（表格形式：信源名|类型|状态码|能否解析|修复建议）
2. 提供修复后的完整代码文件 (clues.py 或拆分后的模块)
3. 提供配置说明 (如需调整环境变量或添加新信源)
4. 提供测试命令或脚本，验证采集功能

【验收标准】
- 至少 70% 的 RSS 源能够正常采集
- 至少 80% 的 API 源能够正常采集
- 采集成功率 > 80% (成功采集数/尝试采集数)
- 搜索结果必须包含搜索关键词
- 多次搜索无重复数据
- 系统能够优雅处理失败情况，不影响其他信源
- 代码通过类型检查，无明显的语法或逻辑错误
- 首页加载无500错误

请基于以上要求，分析现有代码并给出修复方案。如果需要，可以先输出诊断报告，再输出修复代码。
```
