# 调整清单

本文档记录项目后续还需要继续完善、修复或答辩时需要说明的点。

## 一、已完成的近期调整

1. AI 稿件生成页面已增加自定义提示词输入能力，用户可以输入额外写作要求，并和标题、关键词、正文要求一起发送给 AI。
2. 新闻线索采集已调整为只保留能采集到有效线索的来源。
3. 线索采集中的去重逻辑已删除，避免采集结果被过度过滤。
4. 启动脚本已针对 MySQL 用户权限问题做过修复，启动时会尽量确保应用账号可连接数据库。

## 二、仍需重点完善的问题

### 1. 读者消息中心字段需要统一

当前后端消息模型和前端页面使用的字段不完全一致。前端会使用 `type`、`is_read` 等字段，但后端模型较简单，容易导致消息类型、未读状态、回复状态显示不完整。

建议：

- 后端 `messages` 表补充消息类型、是否已读、接收人等字段。
- 前端接口返回结构与页面渲染字段保持一致。
- 补齐标记已读、回复消息等真实落库逻辑。

### 2. 图片上传能力需要继续完善

项目中存在上传入口，但部分上传逻辑仍偏占位。毕业设计演示时如果要展示头像、封面图或稿件插图，需要保证上传后可以稳定访问。

建议：

- 统一上传目录。
- 返回可访问的图片 URL。
- 增加文件类型和大小校验。

### 3. 部分统计和反馈接口仍需增强

反馈趋势、收藏、部分统计数据可能仍有演示数据或空实现。答辩演示可以先聚焦核心闭环，但后续完善时需要让统计数据真实来自数据库。

建议：

- 浏览、点赞、评论全部真实落库。
- 首页统计从真实表聚合。
- 趋势图按日期生成真实数据。

### 4. 后端服务层可以继续拆分

部分业务逻辑仍集中在路由或较长的服务文件中，后续可继续拆分为更清晰的领域服务。

建议：

- 线索采集按来源拆分。
- AI 服务按“生成、润色、预审、线索分析”拆分。
- 路由层只负责参数校验和响应组装。

### 5. 前端重复页面和旧代码可继续清理

项目经历过多轮修改，可能仍存在旧页面、重复组件、废弃接口封装等残留。

建议：

- 用 `rg` 检查未被引用的页面和组件。
- 保留 `pages/editor`、`pages/review`、`pages/reader` 三端结构。
- 删除确认无引用的旧代码前，先跑构建验证。

## 三、答辩演示优先级

建议答辩时优先展示最完整的主流程：

1. 登录采编端。
2. 采集新闻线索。
3. 选择线索并生成稿件。
4. 在 AI 稿件生成中输入自定义提示词。
5. 保存稿件并提交审核。
6. 审核端查看稿件并通过。
7. 读者端查看已发布文章。

这个流程最能体现系统主题：“大模型支持的新闻内容采编系统”。

## 四、后续优化顺序

1. 优先保证启动稳定：MySQL、后端、前端都能通过 `./start.sh` 正常运行。
2. 优先保证核心链路稳定：线索 → AI 生成 → 稿件 → 审核 → 发布。
3. 再完善读者互动：评论、点赞、消息。
4. 最后补齐统计图表和工程化清理。

