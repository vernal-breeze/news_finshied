# 对象、问题与创新点

## 一、系统面向对象

本系统主要面向新闻内容生产链路中的三类用户。

| 对象 | 需求 |
|------|------|
| 新闻编辑 / 记者 | 快速发现线索、生成初稿、管理稿件状态 |
| 审核人员 | 快速判断稿件质量、记录审核意见、控制发布风险 |
| 普通读者 | 浏览已发布内容、搜索文章、进行评论和反馈 |

从毕业设计角度看，本项目的核心服务对象是新闻采编机构或校园媒体团队，重点解决内容生产效率、流程协同和智能辅助不足的问题。

## 二、解决了什么问题

### 1. 线索发现分散

传统新闻线索往往来自多个网站、热榜、RSS、公开 API 或人工搜索，信息入口分散，编辑需要频繁切换平台。

本系统通过线索采集模块把可用来源集中到一个页面，并结合 AI 分析新闻价值，帮助编辑快速判断哪些线索值得继续跟进。

### 2. 稿件初稿耗时

新闻写作通常需要先整理素材、搭建结构、提炼标题摘要。对时效性新闻来说，初稿速度会直接影响发布效率。

本系统通过 AI 稿件生成模块，根据标题、关键词、正文要求和用户自定义提示词生成初稿，降低起稿成本。

### 3. 审核流程不透明

传统稿件审核可能依赖线下沟通，状态不清晰，修改意见不集中。

本系统把稿件状态、审核意见、审核结果统一记录在系统中，形成可追踪的稿件流转链路。

### 4. 生产和反馈脱节

很多内容管理系统只负责发布，不重视读者端反馈对采编工作的反向支持。

本系统保留读者端浏览、评论、点赞等反馈能力，为后续内容优化和选题判断提供数据基础。

## 三、市面上不足与本项目切入点

市面上的新闻系统大致有三类：

| 类型 | 常见不足 |
|------|----------|
| 传统 CMS | 偏内容发布，缺少线索采集和 AI 辅助生产 |
| 单点 AI 写作工具 | 能生成文本，但缺少稿件管理、审核发布、读者反馈闭环 |
| 企业级采编平台 | 功能强但部署复杂、成本高，不适合毕业设计和轻量团队使用 |

本项目的切入点是：用较轻量的 Web 系统，把“线索采集、AI 生成、审核发布、读者反馈”串成一个完整闭环，而不是只做一个 AI 写作页面或普通文章管理后台。

## 四、创新点

### 1. 大模型深度融入采编流程

系统不是简单调用 AI 生成一段文字，而是在多个环节使用大模型能力：

- 线索阶段：辅助判断新闻价值和传播潜力。
- 写作阶段：辅助生成标题、摘要、关键词和正文。
- 修改阶段：辅助续写、改写、润色。
- 审核阶段：辅助给出内容质量和风险判断。

### 2. 全流程闭环设计

系统覆盖新闻生产的完整链路：

```text
线索发现 → 选题策划 → AI 写稿 → 稿件管理 → 审核发布 → 读者反馈
```

这个闭环比单纯文章管理系统更贴近真实采编工作。

### 3. 三端分离

系统区分采编端、审核端和读者端：

- 采编端负责内容生产。
- 审核端负责质量控制。
- 读者端负责内容展示和反馈。

三类角色职责清晰，方便答辩时展示权限分离和业务流程设计。

### 4. 多源线索采集

系统对接多种可采集来源，支持从公开新闻源、技术社区、热榜和 RSS 等渠道获取线索，并统一整理为系统内线索数据。

相比手动搜索，这种方式更适合新闻采编场景下的快速选题。

### 5. 轻量化部署

系统使用 FastAPI、React、MySQL 和 Docker Compose，既能体现完整工程架构，也能在普通开发环境中一键启动，适合毕业设计演示和后续扩展。

## 五、答辩表达建议

可以把项目总结为：

> 本系统面向新闻采编工作中线索分散、写稿耗时、审核流程不透明的问题，设计并实现了一个大模型支持的新闻内容采编平台。系统将线索采集、AI 稿件生成、稿件审核发布和读者反馈整合为完整闭环，相比普通 CMS 或单点 AI 写作工具，更强调新闻生产流程的协同和智能化。

